kind = "web"
previewPath = "/"
title = "GhostMap"
version = "1.0.0"
id = "artifacts/ghostmap"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25489

[services.development]
run = "pnpm --filter @workspace/ghostmap run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/ghostmap", "run", "build" ]
publicDir = "artifacts/ghostmap/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25489"
BASE_PATH = "/"
